"""Centralised normalisation, validation and mapping utilities.

Provides a single source-of-truth for categories, severity, confidence,
IOC patterns, entity helpers, enrichment field mapping, text utilities,
geo data, IOC lifecycle management, Diamond Model vertex extraction,
kill chain phase mapping, cross-feed correlation, STIX 2.1 bundle export,
and Sigma detection rule generation used across the worker and API services.
"""
